import './chat.css';

export const metadata = {
  title: 'Ask Pam+ — Group 2',
  description: 'Ask anything about the Pam+ project.',
};

export default function ChatLayout({ children }) {
  return <>{children}</>;
}
